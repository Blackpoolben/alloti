[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 60 --access-logfile - app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
